const corretSquares = {
  beach: { odlaw: [2, 21], waldo: [48, 22], wizard: [16, 20] },
};

const getCorrectSquares = (name) => corretSquares[name];

const data = { getCorrectSquares };
export default data;
